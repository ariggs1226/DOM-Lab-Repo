let addSqBtn = document.createElement('button').name = 'Add Square';
let addSqClk = document.addEventListener('click', addDivBtn());
let addSqHvr = document.addEventListener('hover', showDivID());
let num = 1;

function addDivBtn() {
    let div = document.createElement('div').className = "btnDiv d-flex flex-row flex-wrap align-items-start";
    div.id = num.toString()++;
};

function showDivID() {
    div.
}